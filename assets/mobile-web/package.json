{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run build-terminal.js",
    "check": "bun run build && bun run verify-terminal.js"
  },
  "dependencies": {
    "ghostty-web": "0.4.0"
  }
}
